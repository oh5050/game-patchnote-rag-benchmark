# ---------------------------------------------------------------------------
# 역할: 인덱싱 패키지 초기화 마커.
# 의존관계: embedder(임베딩), vector_store(인덱스 구축/로드).
# ---------------------------------------------------------------------------
