# ---------------------------------------------------------------------------
# 역할: 평가(evaluation) 패키지 초기화 마커.
# 핵심: 검색 평가(search_eval)와 답변 평가(answer_eval)를 분리(decouple).
# 의존관계: metrics, search_eval, answer_eval, judge.
# ---------------------------------------------------------------------------
