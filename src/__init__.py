# ---------------------------------------------------------------------------
# 역할: src 패키지 초기화 마커.
# 입력: 없음
# 출력: 없음
# 의존관계: 하위 모듈(config, llm, indexing, retrieval, generation, evaluation).
# ---------------------------------------------------------------------------
